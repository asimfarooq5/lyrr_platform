# LYRR Platform Backend - Core Dependencies
# 
# Install with: pip install -r requirements.txt
# For development: pip install -r requirements.txt -r requirements-dev.txt

# FastAPI Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.3

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt pinned to 4.0.1: passlib 1.7.4 is incompatible with bcrypt >= 4.1
# (bcrypt removed __about__ and raises on >72 byte passwords)
bcrypt==4.0.1

# Security
cryptography==42.0.0
pydantic[email]==2.5.3
pydantic-settings==2.1.0

# Rate limiting (slowapi) + Redis client (caching, CSRF store)
slowapi==0.1.9
redis==5.0.1

# Utilities
python-dotenv==1.0.0

# Excel export for admin analytics
openpyxl==3.1.2

# EPUB Parsing
beautifulsoup4==4.12.3
lxml==5.1.0
